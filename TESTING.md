# Hermes Web UI：瀏覽器測試計畫

> 本文件供人工瀏覽器測試或 Claude 瀏覽器代理程式（Browser Agent）使用。
> 涵蓋至 v0.50.21 及後續版本的 UI 使用者功能。
> 每個章節皆編寫為逐步測試程序及預期結果。
> 瀏覽器代理程式（例如具備 Chrome 存取權限的 Claude）可直接執行此計畫。
>
> **前置作業：** SSH 隧道已在連接埠 8787 啟動。在瀏覽器中打開 http://localhost:8787。
> **伺服器健康檢查：** `curl http://127.0.0.1:8787/health` 應回傳 `{"status":"ok"}`。
>
> **自動化覆蓋：** 透過 `pytest tests/ --collect-only -q` 已收集 3648 個測試。
> **本地回歸重點：** 驗證先前關閉的工作區面板在重新整理後，從初始渲染到啟動完成皆保持關閉狀態，不應出現短暫開啟後又關閉的閃爍現象。

---

## 如何使用此文件

每個測試包含：
- **SETUP (設定)**：測試前的準備動作。
- **STEPS (步驟)**：執行的編號動作。
- **EXPECT (預期)**：應看到的現象（通過標準）。
- **FAIL (失敗)**：代表測試失敗的情形。

請依序執行各章節，每個章節皆建立在之前的基礎上。

---

## 第 1 節：初始加載與空狀態

### T1.1：全新載入顯示空狀態
**SETUP**：清除 localStorage（開發者工具 > Application > Local Storage > 刪除 hermes-webui-session）或使用無痕模式。  
**STEPS**：
1. 導航至 http://localhost:8787。  
**EXPECT**：
- 深色背景。
- 側邊欄直接以圖示標籤列開始，無專屬品牌標題。
- 中心區域顯示「有什麼我可以幫您的？」標題與建議按鈕。
- 側邊欄會話列表為空或顯示既有會話。
- 側邊欄底部顯示單一的「Hermes WebUI」控制中心按鈕。
**FAIL**：頁面顯示錯誤、空白畫面或在無使用者操作下自動建立新會話。

---

## 第 2 節：會話管理

### T2.1：透過 + 按鈕建立新會話
**STEPS**：
1. 點擊側邊欄中的「+ 新對話」按鈕。  
**EXPECT**：
- 側邊欄出現一個名為「未命名 (Untitled)」的新會話並呈高亮狀態。
- 中心區域顯示空狀態。  
**FAIL**：建立多個會話、拋出錯誤或未顯示空狀態。

### T2.2：發送訊息並查看回覆
**STEPS**：
1. 在底部訊息輸入框輸入：「用正好三個詞跟我打招呼」。
2. 按下 Enter。  
**EXPECT**：
- 使用者訊息立即出現在對話中。
- 下方出現思考中圖示（三個動態點點）。
- 發送按鈕變為停用狀態（灰色）。
- 回覆完成後，思考中圖示消失，發送按鈕恢復。  
**FAIL**：訊息未出現、點點永不消失，或按鈕持續停用。

---

## 第 3 節：模型選擇

### T3.1：模型下拉選單顯示所有選項
**STEPS**：
1. 點擊輸入框底部的模型下拉選單。  
**EXPECT**：
- 可見供應商群組：OpenAI, Anthropic, Other。
- 包含 GPT-4o, Claude 3.5 Sonnet, Gemini 2.5 Pro 等模型。  
**FAIL**：選單選項過少、無群組分類或模型缺失。

---

## 第 4 節：檔案上傳

### T4.1：點擊附件開啟檔案選取器
**STEPS**：
1. 點擊輸入框中的迴紋針圖示。  
**EXPECT**：
- 開啟作業系統檔案選取對話框。  
**FAIL**：無反應或拋出錯誤。

### T4.2：夾帶文字檔並發送
**STEPS**：
1. 選取一個 .txt 或 .py 檔案。
2. 在輸入框上方出現檔案晶片。
3. 輸入「這檔案內容是什麼？」並發送。  
**EXPECT**：
- 使用者訊息顯示文字及檔案標籤。
- Hermes 回覆並描述該檔案內容。  
**FAIL**：上傳失敗或 Hermes 未提到該檔案。

---

## 第 5 節：工作區檔案瀏覽器

### T5.1：開啟檔案面板時載入檔案樹
**STEPS**：
1. 點擊頂部欄的「檔案 (Files)」切換按鈕。  
**EXPECT**：
- 右側面板顯示「工作區 (WORKSPACE)」標題。
- 顯示工作區內的檔案與目錄列表。  
**FAIL**：右側面板空白或發生錯誤。

### T5.3：預覽程式碼檔案
**STEPS**：
1. 點擊檔案樹中的 .py 或 .js 檔案。  
**EXPECT**：
- 右側面板從檔案樹切換為預覽區域。
- 以等寬字型顯示檔案內容（原始碼）。  
**FAIL**：無反應或顯示亂碼。

---

## 第 7 節：工具核准 (Tool Approval)

### T7.1：危險指令顯示核准卡片
**STEPS**：
1. 輸入：「執行指令：rm -rf /tmp/hermes_test」並發送。  
**EXPECT**：
- 出現橘色/紅色的核准卡片：「危險指令 - 需要核准」。
- 顯示「允許一次 (Allow once)」、「拒絕 (Deny)」等按鈕。  
**FAIL**：未出現卡片，代理程式直接執行。

---

## 第 19 節：會話重新命名 (Sprint 4)

### T19.1：雙擊重新命名會話
**STEPS**：
1. 在側邊欄雙擊某個會話標題。  
**EXPECT**：
- 標題變為可編輯的輸入框。
- 輸入框具有藍色邊框，且文字已全選。  
**FAIL**：無反應或直接導航至該會話。

### T19.2：按下 Enter 確認更名
**STEPS**：
1. 輸入「我的新會話名稱」並按 Enter。  
**EXPECT**：
- 輸入框消失，更換為新標題。
- 重新整理頁面後，標題依然維持。  
**FAIL**：標題恢復原狀或 Enter 無反應。

---

## 第 24 節：行內檔案編輯器 (Sprint 5)

### T24.1：以唯讀模式開啟程式檔
**STEPS**：
1. 點擊工作區檔案樹中的任意代碼檔案。  
**EXPECT**：
- 內容以唯讀方式顯示。
- 路徑欄位顯示帶有鉛筆圖示的「編輯 (Edit)」按鈕。  
**FAIL**：內容可直接編輯或無編輯按鈕。

### T24.2：編輯按鈕進入編輯模式
**STEPS**：
1. 點擊「編輯」按鈕。  
**EXPECT**：
- 唯讀視圖變更為可編輯的文字區域 (Textarea)。
- 按鈕變更為帶有磁碟圖示的「儲存 (Save)」。  
**FAIL**：無任何變化。

---

## 第 32 節：編輯使用者訊息與重新生成 (Sprint 8)

### T32.1：編輯圖示在懸停時出現
**STEPS**：
1. 游標懸停在任一使用者訊息泡泡上。  
**EXPECT**：
- 右側出現鉛筆（編輯）圖示。  
**FAIL**：圖示未出現或位置錯誤。

### T32.5：發送編輯會截斷並重新生成
**STEPS**：
1. 修改訊息文字並點擊「發送編輯」。  
**EXPECT**：
- 該訊息之後的所有對話被移除。
- Hermes 針對修改後的文字重新開始串流回覆。  
**FAIL**：舊回覆殘留或發生崩潰。

---

## 第 35 節：語法高亮 (Sprint 8)

### T35.1：程式碼塊具備顏色
**EXPECT**：
- 對話中的程式碼塊具備語法著色（關鍵字、字串顏色不同）。
- 採用深色背景的 Prism Tomorrow 主題。  
**FAIL**：僅顯示純白/純灰文字。

---

## 第 36 節：訊息佇列 (Sprint 8 修補)

### T36.1：忙碌時輸入會將訊息排隊
**SETUP**：當 Hermes 正在回覆時（思考中）。  
**STEPS**：
1. 在串流回覆期間發送新訊息。  
**EXPECT**：
- 出現提示泡泡：「已排隊：[您的訊息]」。
- 右下角出現標記：「1 則訊息排隊中」。  
**FAIL**：訊息被靜默丟棄或觸發錯誤。

---

## 第 19 節：認證與安全性 (Sprint 19)

### T19.1：密碼保護
**SETUP**：設定環境變數 `HERMES_WEBUI_PASSWORD=test` 並重啟。  
**EXPECT**：
- 存取頁面時自動導向至 `/login`。
- 輸入正確密碼後，設定 24 小時有效的 Cookie。  
**FAIL**：無須密碼即可進入或登入失敗。

---

## 斜線指令檢查清單 (Slash Command Parity)

請確認以下指令在 Web UI 中運作正常：

- [ ] `/help` — 顯示可用指令清單。
- [ ] `/new` — 開始新對話。
- [ ] `/clear` — 清除當前對話顯示。
- [ ] `/title <名稱>` — 重新命名當前會話。
- [ ] `/model <名稱>` — 切換模型下拉選單。
- [ ] `/theme <名稱>` — 切換 Web UI 主題。
- [ ] `/workspace <路徑>` — 切換工作區。

---

*最後更新日期：2026 年 5 月 8 日 v0.51.30*
*自動化測試總計：4977 個項目*
